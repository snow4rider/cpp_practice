#include <cstdio>

/**
 * @brief Increments a variable and prints its value.
 *
 * This program initializes an integer variable x to 0, increments it by 1,
 * and then prints the resulting value in a formatted string.
 */

int
main ()
{
  int x{};
  ++x;
  42;
  printf ("The %d True Morty\n", x);
}